---
- name: "Creating CSR for domains {{ ', '.join(domains) }}"
  local_action: shell python roles/letsencrypt/code/acme_compact.py gen-csr --key "keys/{{ key_name }}.key" --csr "keys/{{ key_name }}.csr" --domains "{{ ','.join(domains) }}"
  run_once: True
